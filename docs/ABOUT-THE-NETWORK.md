# About this network

A short, public note for residents. It is meant to be copied onto a flyer, a meeting handout, or
your network's front page — written for neighbors, not engineers. Edit the bracketed parts for your
own network. A network operator's setup guide is in
[`ADD-YOUR-NEIGHBORHOOD.md`](ADD-YOUR-NEIGHBORHOOD.md).

---

## About [your network name]

This is a neighborhood-run network of small sensors that measure **heat and air quality across the
neighborhood**, so we can see which areas get hottest and where the air is worst. It is run by us — the
people who live here and host the sensors — not by a company. The readings are open data anyone can
download and check.

**Coverage.** We currently cover [N] locations across [neighborhood / area], shown on the map. Sensors
are added as neighbors volunteer to host them, and we aim for overlapping coverage so the map keeps
working even if one sensor goes offline. Want a sensor near you? [contact / meeting details].

**What the map shows.** Each marker is a *location*, named for a place you know — tap one for its
reading. Air quality leads with the **AQI** number and a plain category (for example "Moderate"),
with a short "What is AQI?" explainer and health guidance from the US EPA (in English and Spanish).
You can read temperatures in °F or °C.

**The AQI is hourly.** The air-quality number we show is an **hourly** reading — a recent snapshot,
not the official 24-hour average — and the map's legend says so. It is meant to tell you what the air
is like *right now* in a given area.

**What "coarse location" means.** We never publish where a sensor actually sits. Every location is
rounded to roughly a [150-meter] grid cell before it appears anywhere, so the map can answer "is this
area hot?" without revealing "there's a sensor on this house." Hosts' homes stay private by default;
a sensor's exact spot is only ever shown if that host specifically asks for it.

**Provisional readings.** A location whose sensor has not yet been calibrated against a trusted
reference is marked **provisional** and shown plainly as not-yet-confirmed. We never present an
unverified reading as a confirmed fact, and a provisional location never claims an air-quality category.

**The data is yours.** Every reading is dedicated to the public domain (CC0): you can download it,
check our work, or take a full copy and run the network somewhere else. There is no company that can
switch it off or fence the data away from the neighborhood that made it.

Questions, corrections, or want to get involved? [contact / meeting details].
