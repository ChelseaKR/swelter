# OSV-Scanner ignore list.
#
# This file is NOT the record of why anything is accepted -- waivers.yml is.
# Every id below must have a live `kind: dependency-advisory` waiver naming
# osv-scanner, with the same date as `ignoreUntil`, or `make security-waivers`
# fails. Adding an id here on its own does not silence it; it breaks the build.
#
# Every advisory not listed here still fails `make security-osv`.

[[IgnoredVulns]]
id = "GHSA-jmr9-qjv8-65gv"
ignoreUntil = 2026-11-15
reason = "waivers.yml WVR-001 -- unpatched symlink traversal in extract-zip 2.0.1, reachable only as a transitive devDependency of the pa11y accessibility toolchain in web/, never shipped; the only remedy offered is a breaking downgrade of pa11y. Re-review by 2026-11-15."
